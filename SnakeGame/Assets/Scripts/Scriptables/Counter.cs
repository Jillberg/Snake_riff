using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName ="newCounter",menuName ="Counter")]
public class ItemCounter : ScriptableObject
{ 
    public int counter ;
    // Start is called before the first frame update
   
}
